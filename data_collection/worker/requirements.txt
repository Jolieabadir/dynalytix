# Runtime for the pose worker (data_collection/worker). The Modal image installs
# exactly this file; a laptop needs the same to run tests/ on CPU.
mediapipe==0.10.32
opencv-python-headless>=4.8
numpy>=1.24,<3
boto3>=1.34
httpx>=0.27
psycopg[binary]>=3.1
# Only for the `enqueue` web endpoint (modal.fastapi_endpoint).
fastapi[standard]>=0.110
